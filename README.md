# a

`canvas_todo.py`: lists Canvas assignments not submitted in the past N days and due in the next N days.

```
export CANVAS_TOKEN=...   # Canvas -> Account -> Settings -> New Access Token
python3 canvas_todo.py
```
